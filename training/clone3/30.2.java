class Shit {
 void method(){
         for (int j = 3; j < ass.length; j++) {
						System.out.print(j+" "+k+" ");
            if (j < k)
                ass[i] = j%k;
            else if (j == k)
                ass[i] = k;
            else
                ass[i] = k - (j % k);
						System.out.print(ass[i]);
        }
 }
 }
