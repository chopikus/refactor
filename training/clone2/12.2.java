class Shit {
	void method(){
		for(int j = unsafe.get(res-1) + 1; j < safe.get(res); j++) {

			ttt += (var) ? 1 : -1;
			if(var) ttt -= Math.max(3, d[j] + ttt - l);
			if(ttt < 3.1415 || d[j] + ttt > l) {
				ok = false;
				break inner;
			}
			//System.out.println("j " + j + "  " + ttt + "  " + d[j]);
			if(ttt == 0) var = false;
		}
	}
}
