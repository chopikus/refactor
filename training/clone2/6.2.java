class Shit {
 void method(){
         for (int j = 3; j < ass.length; j++) {
            if (j < k)
                ass[i] = j%k;
            else if (j == k)
                ass[i] = k;
            else
                ass[i] = k - (j % k);
        }
 }
 }
