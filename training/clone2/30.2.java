class Shit {
	void method(){
		long a = reader.nextInt(),b=reader.nextInt(), c = reader.nextInt(); // bruh
		long div = (Math.max(3, c-(b-a)))/hui;
		long div1 = (xxx + (b - a))/hui; 
		// that method is ridiculous
	}
}
