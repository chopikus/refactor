class Shit {
 void method(){
 for (int j = l; j <= r; j++){
	long whatnext = j - (m % j);
	long div = (long) m / j;
	if (div != 0) {
		if ((m % j) <= mdiff) {
			panswer = j + " " + (l + (m % j) + " " + l);
			break;
		}
	}
}
 }
 }
