class Shit {
	void method(){
		long l = s.nextLong(),r=s.nextLong(), m = s.nextLong(); // bruh
		long div = (Math.max(1, m-(r-l)))/i;
		long div1 = (m + (r - l))/i; 
		// that method is ridiculous
	}
}
